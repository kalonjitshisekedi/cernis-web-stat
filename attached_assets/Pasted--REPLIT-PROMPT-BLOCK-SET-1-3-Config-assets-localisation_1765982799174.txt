## REPLIT PROMPT — BLOCK SET 1/3 (Config + assets + localisation sweep)

You are continuing work in an **existing codebase** for the Cernis website. **Do not rebuild. Do not change frameworks. Do not delete working features.** Make minimal, safe edits.

**Production domain:** `https://cernis.co.za`

### 1) Add a single canonical site URL source

* Add `SITE_URL` support (env-first, fallback to local dev URL).
* Use this value everywhere URLs must be absolute (SEO, RSS, sitemap).
* Ensure canonical link and OpenGraph URL use `SITE_URL`.

### 2) Fix static assets loading in nested routes

* Audit all templates/layouts/components for asset paths:

  * `<link href>`, `<script src>`, `<img src>`, CSS `url(...)`
* Fix any relative paths that break on blog post pages or nested routes.
* Prefer root-absolute paths (`/assets/...`) or the framework’s official asset helper.
* Must work locally and after S3 deployment.

### 3) Final UK → South Africa localisation sweep (only what remains)

* Replace remaining UK domain references with `cernis.co.za`
* Replace remaining UK phone formats with `+27`
* Replace UK address/location references with South African locations
* Update footer/header/contact page + any SEO metadata/structured data fields.

### Verification commands (run and show output)

```
grep -R "co.uk" -n . || true
grep -R "+44" -n . || true
grep -R "London" -n . || true
```

### Output required

* List files changed + short reason per file.
* Confirm assets load on:

  * homepage
  * blog index
  * a blog post page

---

## REPLIT PROMPT — BLOCK SET 2/3 (RSS + sitemap canonicalisation)

Continue in the same codebase. **No rebuild. No framework changes.** Ensure all generated outputs are production-ready.

### 4) RSS feed: make links absolute + validate structure

* Ensure `output/feed.xml` is valid RSS.
* All `<link>` and `<guid>` values must be absolute using `SITE_URL`.
* Each blog item includes: title, description/summary, pubDate, link, guid.
* Ensure 2–3 posts appear.

### 5) Sitemap: absolute URLs + include all key pages

* Ensure `output/sitemap.xml` uses absolute `<loc>` URLs via `SITE_URL`.
* Include:

  * `/`
  * key service pages
  * blog index
  * each blog post
* Ensure no UK domain appears anywhere.

### Verification commands (run and show output)

```
cat output/feed.xml | head -n 60
cat output/sitemap.xml | head -n 60
grep -R "co.uk" -n output || true
```

### Output required

* Confirm RSS contains blog posts and absolute URLs.
* Confirm sitemap contains blog posts and absolute URLs.

---

## REPLIT PROMPT — BLOCK SET 3/3 (SEO consistency + blog content + docs)

Continue from current state. **Do not rebuild.** Polish for production.

### 6) SEO consistency across all pages

* Ensure base layout sets:

  * Title pattern: `Page title | Cernis`
  * meta description present
  * OpenGraph title/description/url (absolute)
* Add/confirm structured data:

  * Organisation schema with South Africa details + canonical URL
  * BlogPosting schema per post if supported (optional but preferred)

### 7) Blog content: ensure SA relevance + inclusion everywhere

* Confirm 2–3 SA-focused blog articles exist and render correctly.
* Ensure they are included in:

  * blog index
  * RSS feed
  * sitemap
* Topics should be practical for SA businesses (POPIA, cloud cost optimisation, Microsoft 365/Copilot adoption, etc.).

### 8) Documentation final update

Update README/docs to include:

* install/run steps
* how to set `SITE_URL`
* build/export steps
* how to verify RSS + sitemap
* S3 + custom domain deployment overview (minimal cost)

### Verification commands (run and show output)

```
ls -la output || true
```

### Output required

* Summary of SEO defaults + per-page overrides.
* Confirm docs updated and accurate.
